# MIN-007E implementation plan

- Add strict candidate, scenario, marginal, projected, blocked, and invalid result models.
- Implement the frozen Decimal SHA-256 exponential-race algorithm and exact phase/capacity semantics.
- Add unit, property, and golden regression coverage against the supplied sampler oracle.
- Run the 15 literal acceptance commands, record factual evidence, commit exactly once, and verify a clean tree.
