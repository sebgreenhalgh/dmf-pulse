# L1 offline acceptance

Classification: generated/synthetic or approved retained public evidence only.
No live FPL/Odds access, real credential inspection or private attempt occurred.
The inherited PostgreSQL population is an exact-SHA CI gate, not a new L1 database
requirement. All commands below run in the isolated L1 worktree.

## Completed checks

| Command/population | Result |
| --- | --- |
| `uv run --offline ruff format --check .` | PASS, 888 files formatted |
| `uv run --offline ruff check .` | PASS |
| `uv run --offline mypy src/dmf_pulse` | PASS, 311 source files |
| `uv sync --all-groups --frozen --offline` | PASS, 40 packages |
| `uv build --no-sources --offline` | PASS, wheel and sdist |
| `uv run --offline python scripts/verify_team_strength_l1_wheel.py` | PASS, external clean runtime-only environment; installed import, exact authority, mutable-source rejection, wrong-purpose pre-provider block, operator help; zero provider calls |
| `uv run --offline pytest -q tests/unit/private_v1/test_team_strength_shadow_inputs.py tests/unit/private_v1/test_team_strength_shadow_comparison.py tests/unit/private_v1/test_team_strength_shadow_cases.py tests/unit/private_v1/test_one_command.py` | 64 passed, 1085.24s |
| `uv run --offline pytest -q tests/unit/private_v1/test_team_strength_l1.py tests/unit/private_v1/test_team_strength_l1_cli.py tests/unit/private_v1/test_a2_preparation.py` | 74 passed, 424.22s |
| `uv run --offline pytest -q tests/unit/ingestion/openfootball tests/unit/football_events/test_team_strength_model.py tests/unit/football_events/test_team_strength_numerics.py tests/unit/football_events/test_team_strength_hardening.py tests/unit/football_events/test_team_strength_adapter.py tests/unit/ingestion/test_horizon_rights_approval.py tests/unit/ingestion/test_horizon_probe.py` | 516 passed, 205.02s |
| `uv run --offline coverage run --append --branch --data-file=review_pack/l1.coverage -m pytest -q tests/unit/ingestion/openfootball/test_team_strength_current.py` | 17 passed, 234.86s |
| `uv run --offline coverage run --branch --data-file=review_pack/l1-extra.coverage -m pytest -q tests/unit/private_v1/test_team_strength_l1.py -k 'post_cutoff or audit_hook or repeated_acquisition'` | 6 passed, 41 deselected, 142.78s |
| `uv run --offline python scripts/generate_repository_manifest.py --ticket PRC-013` | PASS, canonical current manifest regenerated |
| `uv run --offline python scripts/validate_repository.py` | PASS, zero errors |
| `uv run --offline python scripts/scan_secrets.py` | PASS, zero findings |
| `git diff --check` | PASS |
| `uv run --offline python scripts/build_team_strength_l1_review_pack.py` | PASS, 27 explicitly allowlisted source/offline-evidence files; no runtime artifacts |

Principal L1 coverage population included the complete generated provider-shaped
vertical slice: actual Stage-7 fit and both unchanged canonical Stage-8 through
Stage-11 solves. It passed under branch instrumentation and arbitrary-destination
filesystem-write denial. It verifies one preparation, one FPL session, one Odds
horizon acquisition, one league-prior acquisition, exact controls, zero network
delta, safe movement/decision summaries and no activation. Only offline test
scenario draws are reduced to two; the production request retains 256 and the
inherited exact optimiser/search policy.

Combined ticket-owned branch coverage: **98 / 104 = 94.23076923076923%**;
statements 405 / 410; excluded lines zero. The machine-readable per-file proof is
`coverage-summary.json`. The combined coverage file is generated by:

```
uv run --offline coverage combine --keep --data-file=review_pack/l1-combined.coverage review_pack/l1.coverage review_pack/l1-extra.coverage
uv run --offline coverage json --data-file=review_pack/l1-combined.coverage -o review_pack/l1-coverage-final.json --include='*/team_strength_current.py,*/team_strength_live.py,*/team_strength_live_authority.py,*/team_strength_live_network.py'
```

## Resolved verification issues

- Two new freshness-mask tests initially resealed source lineage without resealing
  nested match identities. Those tests were corrected to construct a coherent
  adversarial source. Both independently reran PASS and the complete 17-test public
  population passed. No source authentication was relaxed.
- A synthetic HTTP retry fixture initially omitted required quota headers; the
  fixture was fixed. Both normal two-attempt and explicit one-attempt behaviours
  pass for network exceptions and HTTP retry responses.
- A local transport run initially included four PostgreSQL tests without a local
  `DMF_TEST_DATABASE_URL`. They were not represented as passes. The non-PostgreSQL
  population passed (207 passed, four deselected); inherited PostgreSQL acceptance
  remains mandatory in CI.

## Preservation and publication

Diff against exact parent is empty for accepted 001P comparison/input modules,
ordinary one-command/rolling/service/CLI paths, football-event mathematics, FPL
rights and OpenFootball rights. Odds changes are limited to the human purpose
metadata and an explicit per-client one-attempt narrowing; ordinary retry default
is unchanged. CI sharding changes are static balancing weights only.

Final independent review, remote equality, all mandatory exact-SHA CI jobs and
clean worktree are distinct gates. CI/post-commit facts are reported at handoff
without recursively modifying the sealed tree. No Phase-B result belongs in this
repository evidence file.
