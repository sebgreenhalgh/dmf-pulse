# MIN-007R5G result

Status: COMPLETE.

MIN-007R5G closes AUDIT-007-3 findings 9–12. The requested UTC `as_of` is now validated against the training cutoff and mapped kickoff, propagated through the effective context/public result/registry signature/persisted run, and invalid cutoffs publish nothing. The exact `MIN-007-COHERENCE-V1` policy seed is enforced at the CLI and Python boundary without being reinterpreted as a sampler suffix.

The approved byte-identical synthetic mapping plan covers 701–709, with 701–708 projected and 709 typed `INSUFFICIENT_ELIGIBLE_SQUAD` blocked. The approved Stage-7 root probability schema is installed at `public_contracts/probability.schema.json`; its SHA-256 is `6a0dcfb79f5e8939dd54f889b61236783d8c4e05a4bd0272eae25599c2373f9b`.

Portable repository goldens no longer use external pack paths. PostgreSQL assurance proves the frozen 701 graph, 23 final player rows, exact reload identity, idempotent repeat, same-signature altered-output rejection, and zero final rows for blocked 709. Alembic remains at `20260807_0006`; no migration was added.

The 23-command acceptance ledger is recorded in `acceptance_matrix.json`: all commands passed with zero skipped tests, the repository validator and secret scan are clean, and the disposable PostgreSQL resources were removed before handoff. The required single commit has parent `2986433e32d55aa3153003b3cc98a098c3a9c071` and message `MIN-007R5G harden CLI mapping and portable assurance`.
