# MIN-007R5G execution plan

## Scope

Close only AUDIT-007-3 findings 9–12 on `stage/A7/MIN-007-basic-minutes-model`, preserving the MIN-007F/G model, registry, migration, and numerical identities.

## Work sequence

1. Re-run focused CLI and G golden tests before editing and inspect current implementation/assets.
2. Install byte-identical approved mapping/schema authorities and make G fixtures repository-portable.
3. Enforce strict requested `as_of`, exact policy seed, data-driven mapping, typed validation, and side/context resolution without changing formulas.
4. Expand persistence and CLI assurance for all nine synthetic scenarios, invalid-input no-publication behavior, and PostgreSQL lineage/idempotency/integrity.
5. Run the 23 literal acceptance commands in order, capture machine-readable evidence, validate the capped review ZIP, commit exactly once, and verify the final clean worktree.

## Invariants

- Alembic head remains `20260807_0006`; no migration is created.
- Frozen F/G artifact, evaluation, and registry identities remain unchanged.
- No live network/provider request or real credential access.
- Public output remains JSON on stdout; diagnostics remain on stderr.
