# R5 scope and correctness review

The exact parent is `56a0776e8e13bfae31297b6b40f631361e1fce95`. Its CI run `34259589435`
was rechecked on 2026-09-10: completed/success, all 12 jobs successful, before editing.
The unrelated dirty root worktree is preserved; R5 uses an isolated worktree.

Parent diagnosis: `service.py::_bounded_private_incoming_ids` receives only the current
projection; `_certified_pointwise_dominated` examines only its scenarios. `_stage11_request`
then copies the resulting incoming set to every node. Future-valued players can therefore
be absent from both root and continuation action spaces before the exact solver runs.

Authority resolves through A10/A11/B2 in `specs/manifests/authority_manifest.json`, DMFP-12
sections 0.8, 8.3--8.6 and 25, and DMFP-20 ADR-OPT-001 through 006. Heuristic candidate pools
must be disclosed; exact tactical/state economics and nonanticipativity must remain intact.
The 001K one-GW and R2/R4 execution contracts remain active within their unchanged scope.

The V2 screen forms one horizon union from per-GW expected/upside/value buckets, additive
horizon expected/value buckets, cheapest-price route escapes and the accepted one-GW screen
for counterfactual compatibility. All boundary ties survive or the complete request fails.
Position universes of at most four remain exhaustive. No future eligibility/provider data is
invented. Incoming identities, prices, all projection hashes, the horizon, bound and bucket
memberships bind the screen hash; the request includes that hash in its assumptions. Existing
node information/state keys already bind allowed incoming sets. No solver key changes needed.

Conservative dominance: no horizon removals are certified. Same club/position, cheaper price,
matching appearances and all-GW scenario superiority alone do not prove replacement is safe
when the manager can own both players or uses distinct ownership/transfer routes. Tests retain
the certificate false negatives rather than mislabel heuristic exclusions as mathematical.

The 24-player V2 STANDARD safety budget is explicit and unchanged numerically. The 600-player
synthetic profile fits: R4 retains 8, R5 retains 9. Adversarial distinct-bucket and boundary-tie
unions can exceed 24 and are explicitly rejected. There is no fallback truncation and no
claim that the union always fits. The bound and exact-search resource limits are not a global
feasibility/runtime guarantee. Expanding the budget without evidence is deliberately avoided.

Reduced five-incoming-player oracles test a future-valued root transfer, hold followed by
two-FT recourse, a cheap budget route and a saturated-club release. R4 screening excludes the
future star; R5 exposes the better policy. Screened versus full-universe comparisons cover
every non-identity economic/tactical field, including ownership-cohort prices. Different
candidate universes correctly produce different information/state/spell identifiers. Within
one request, accelerated and generic candidates must be exactly equal without normalization.

The one-GW ranking, dominance and bounded-screen function source bodies were compared with
the parent and remain identical (normalized line endings). The only one-command code change
is the three-GW screen-version declaration. The previous V1 execution-input label is accepted
for input compatibility, but output names the actual V2 or explicit operator candidate scope.
Automatic future FT scope, actual-action counterfactual and decomposition remain R4 behavior.

`benchmark.json` is wholly synthetic. It separates a small exact oracle benchmark from the
600-player/15-owned-player retained search. Timings use a synthetic tactical evaluator, not
football modelling or a claimed live Stage-10 speed. Every measured solve completed; R5
fast/generic candidate equality is checked in the reduced benchmark. Profiles record FT/count
actions, states, memo hits/misses and unique batched tactical node/squad evaluations.

No independent external review was performed. The inherited canonical review pack does not
register these private revision tickets; a supplementary capped, hash-checked local bundle
is not a canonical acceptance certificate. Human acceptance remains separate from CI.
