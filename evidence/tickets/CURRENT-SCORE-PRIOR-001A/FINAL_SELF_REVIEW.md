# CURRENT-SCORE-PRIOR-001A engineering self-review

## Adversarial review

- Rights: unavailable, altered or insufficient authority blocks before transport with exact zero
  calls. The accepted approval, purpose, provider, terms, retention, geography, account and all
  capability decisions are exact.
- Transport: only HTTPS, the approved host, exact commit and four paths are reachable. Redirects,
  status failures, TLS failures, timeouts, unexpected content, oversize and truncation are typed.
- Data: byte count, Git blob SHA-1 and SHA-256 precede parsing. Duplicate keys, floats, Booleans,
  strings, negative/out-of-range scores, unknown shapes, invalid periods, duplicate fixtures and
  incomplete seasons fail closed.
- Time: approval precedes operation; every receipt and validation precedes `usable_at`; all are no
  later than the cutoff. A failed HTTP attempt increments the call count; pre-transport refusals do
  not.
- Semantics: exact rates bind to the existing home/away `ScorePriorRequest`; no market module is an
  input and no current-strength or production claim is made.
- Binding: an authenticated bundle binds the exact fixture, competition, oriented teams and
  as-of cutoff; identity substitution and a source usable after the cutoff fail closed.
- Authentication: result, bundle and summary semantic hashes recompute during public validation;
  nested source and stale-hash mutations block.
- Import boundary: OpenFootball imports the market-free leaf request contract; the legacy Stage-8
  export is the identical class and frozen GCS schemas remain equal.
- Unexpected transport failures: ordinary injected exceptions are translated without upstream
  text or arguments and retain calls 1-4; `BaseException` control signals are not intercepted.
- Disclosure: the CLI emits a bounded safe summary, not raw matches, licence content or headers.
- State: no database, filesystem persistence or environment mutation occurs in the capability.

## Conclusion

- P0 found/unresolved: 0 / 0.
- P1 found/unresolved: 0 / 0.
- Independent material P2 findings CSP-IR-001/002/003: 3 / 0 engineering-unresolved.
- Independent P3 finding CSP-IR-004: 1 / 0 engineering-unresolved.
- New P0/P1/material P2/P3 found in remediation self-review: 0 / 0 / 0 / 0.

This is an engineering self-review only. Independent review and all later governance remain
separate.

All four dispositions are `ENGINEERING_CLOSED_PENDING_INDEPENDENT_REREVIEW`; this is not an
independent closure claim.

`CURRENT_SCORE_PRIOR_001A_REMEDIATED_PENDING_INDEPENDENT_REREVIEW`
