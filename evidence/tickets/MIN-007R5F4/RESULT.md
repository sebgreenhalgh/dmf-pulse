# MIN-007R5F4 result

PASS. The final-output COMPLETE transition now derives public player, team,
result, and output identities from durable rows and authoritative run data.
All five direct-SQL adversarial declarations are rejected, while the exact
durable result completes and remains frozen. Committed core DRAFT runs are
hidden from exact, facade, as-of, and latest lookup paths; a COMPLETE core with
a final DRAFT exposes no final-player rows.

All 23 literal acceptance commands passed. The migration head remains
`20260807_0006`; the migration suite passed 63 tests; availability passed 148
tests; availability/markets contracts and goldens passed 79 tests; repository
validation and secret scanning passed; and the disposable PostgreSQL service
was removed before completion.
