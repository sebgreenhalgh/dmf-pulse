# PRIVATE-V1-ONE-COMMAND-001L final self-review

## Exactness and scope

- The worktree is rooted at immutable parent `9ce56b93502b894a5c2763d6f19e013714336b00` on the
  required isolated branch.
- The retained 001K evaluator remains present as the oracle and the ordinary single-squad adapter
  path remains unchanged apart from shared result sealing.
- Golden absence, formation, bench, captain fallback, tied-optimum and nonuniform-weight cases
  compare the complete old/new return values, including canonical plans and hashes.
- Overlapping multi-squad batch results equal sequential adapter results and are invariant to
  shuffled and duplicate squad input order.
- A frozen private execution compares the entire Stage-11 optimisation result old versus new.
- Final selected tactics always pass canonical scenario re-evaluation; a mismatch fails closed.

## Performance and counters

- The exact 263-by-256 benchmark is 101.7291795x faster and completes Stage 10 in 14.8504517s.
- A separate 256-unique-global-appearance-mask stress completes the same accelerated squad batch
  in 14.9435100s because exact scenario regrouping is scoped to each relevant 15-player squad.
- Shared caches are node-local and depend only on scenario/rules/player constants or canonical
  tactical structures. No provider or secret object is retained or sent elsewhere.
- Solver status continues to report the complete logical tactical and scenario operation bounds;
  separate kernel work counters describe physical factorisation without weakening the proof.

## Safety

- No dependency, ruleset, candidate-screening, action-generation, provider, persistence or
  activation boundary changed.
- No parallelism was needed to meet the live-shaped target, so worker scheduling and secret
  transport were not introduced.
- No PR, merge, tag, activation, provider write or private-input persistence was performed.
