# PRIVATE-V1-ONE-COMMAND-001K implementation summary

- Replaced the fixed one-transfer automatic policy with a manager-state and compiled-rules-derived
  0/1/2 private STANDARD scope while preserving the complete selectable incoming universe.
- Added certified same-club/position/appearance pointwise dominance plus the explicit
  `PRIVATE_CURRENT_TRANSFER_CANDIDATE_PRUNING_V1` expected/upside/value shortlist. Ranking ties are
  retained; more than 24 retained candidates fails closed instead of silently cutting by ID.
- Kept the entire current player catalogue and exact manager squad in the Stage-11 request. The
  shortlist changes only the root transfer-in scope.
- Changed generic exact action enumeration to generate position-compatible incoming combinations
  directly, while retaining canonical affordability, club, ownership, duplicate and purchasable
  validation before tactical evaluation.
- Counted the legal retained 0/1/2 actions and unique resulting squads before optimisation, exposed
  every required safe progress line, and reused the existing per-root canonical Stage-10 cache.
- Corrected multi-transfer display pairing by position and added bounded-scope plus zero-terminal-
  value disclosure to the human one-command report.
- No Stage 7-9, penalty, captaincy, chip or acquisition behavior changed.
