# Scope assurance

Changed product scope is limited to `src/dmf_pulse/prices/`, the `dmf prices` CLI registration,
and strict Stage-13 configuration. Assurance changes add the exact false-positive allowlist entry,
post-push review-pack builder, PRC-013 active-manifest selection and refreshed current manifest.
Cross-stage behavior is consumed through accepted Stage-11 and Stage-12 interfaces. There is no
migration, database, provider transport, model server, UI, autonomous execution, chip scheduler,
rank/EO strategy, unrelated football model, rule activation, PR, merge or accepted tag.
