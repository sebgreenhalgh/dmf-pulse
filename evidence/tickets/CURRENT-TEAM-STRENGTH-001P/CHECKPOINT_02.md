# 001P.02 — authenticated prior seam

Private-only resolver injection retains the literal default rolling service when omitted.
The separately sealed shadow input binds the original execution, full accepted model artifact,
fixture registry and all nine oriented fixture bundles. Existing P0 season-scoped official-FPL
crosswalks bridge public UUIDv7 evidence to private transient identifiers without rematching.
No ordinary constructor factory, CLI, environment/config selection, market mathematics,
availability mathematics, player allocation mathematics or optimiser changed.

Preparation is all-or-nothing: missing current artifact/assessment, stale source and incomplete
fixture coverage return explicit unavailable outcomes. Integrity, cutoff, nested authentication,
registry and oriented identity failures raise. DEGRADED explicitly reuses the sealed artifact.
Reconstructed artifacts cannot claim a real current private world. Simulated LIVE_OBSERVED
tests use wholly generated score bytes, never reconstructed real final files.

## Direct checks

- Full new input population: 28 cases, including two real canonical rolling solves.
- Combined inherited/new population: 35 passed; one inherited exact-constructor-signature
  guard initially rejected the new private keyword. Updated that guard to require exactly
  the two private resolver keywords, both defaulting to None; isolated rerun passed.
  No behavioral assertion was removed. Original population took 176.02 seconds.
- Combined direct coverage: 162/162 statements and 48/48 branches (100%). No exclusions.
- Ruff and strict mypy: pass (305 production source files).
- Separate-process exact-f39 baseline regression: PASS; see `baseline_semantic_regression.json`.

The cross-build regression discovered an existing deliberate boundary: market normalisation
binds the **entire installed Python package** through `code_identity()`. Its authenticated
hashes necessarily differ after adding modules. The proof rebuilds markets under each actual
build, verifies identical non-hash market content, and uses the ordinary service in each process.
It compares all non-hash execution, projection, decision, report, optimiser request/result and
work-count content. Propagated SHA values and information-set content IDs (bijective to their
Gameweek) are normalised only in the comparison. Production verification is not bypassed,
patched or weakened. This is semantic equality, not byte/hash identity across builds.

Commands:

```text
uv run --frozen --offline python -m pytest tests/unit/private_v1/test_a1_allocation_injection.py tests/unit/private_v1/test_team_strength_shadow_inputs.py -q --junitxml=review_pack/001p-seam.xml
uv run --frozen --offline python -m coverage run --append -m pytest tests/unit/private_v1/test_team_strength_shadow_inputs.py -q
uv run --frozen --offline python -m coverage json -o review_pack/001p-input-coverage.json --include=*/team_strength_shadow_inputs.py
uv run --frozen --offline python scripts/verify_team_strength_shadow_baseline.py --private-parent-root <exact-f39-worktree> --report evidence/tickets/CURRENT-TEAM-STRENGTH-001P/baseline_semantic_regression.json
```

Coverage combines the initial 21-case run, the extended 25-case population excluding the already
covered real solve, and the two newly explicit mapping/orientation failure probes. The full
new population is rerun with inherited allocation-seam/default-service regressions before seal.
This is an intermediate checkpoint, not final comparison acceptance or activation authority.
No live provider calls or private live persistence occurred.
