# Inherited CI baseline

This checkpoint does not change Odds/provider code. The pre-existing general
CI failure remains
`tests/integration/ingestion/odds/test_live_provider_current_input.py::test_live_provider_native_input_persists_governed_evidence_without_mapping`,
documented as `QUOTA_REQUEST_COST_MISMATCH` in run `32433733490`. It is outside
GW1-PLY-002 scope and is not masked or reclassified as a role-prior result.
