# MIN-007H3 final assurance

This directory contains durable, fail-closed assurance evidence rebuilt after
the R7 packaged-resource repair. The ledger covers pre-commit gates only and
binds every critical generated artifact by byte size and SHA-256. Installed-wheel
and network evidence execute the public external-ID-701 console command from a
disposable runtime outside the repository. Post-commit Git range material is
generated directly by the review-pack builder and is not committed here.
