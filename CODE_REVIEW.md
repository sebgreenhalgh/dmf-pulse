# DMF Pulse review standard

Review FPL-004 in this order:

1. Scope and authority: exact Pack 004 hashes, controlling rights register, approved decisions, frozen synthetic oracles, and no excluded provider/model/account functionality.
2. DAT remediation: immutable retrieval envelope plus append-only derived lifecycle, deterministic resume, fixture competition/season and fixture/Gameweek database coherence, semantic schema hash, rules identity, subjectful quality, and raw rights/storage separation.
3. Rights and retention: immutable profile/version decisions at every enforcement point; unknown means deny; official manual handling remains transient; synthetic fixtures alone authorize persistent promotion/bundling.
4. Transport and security: rights check precedes construction/call, denied snapshot has zero calls, endpoints are allowlisted, failures bounded/typed, and no body/URL/header/credential leak or residual temporary file exists.
5. Parsing and mapping: strict required fields/types/UTC/Decimal behavior, bounded depth/size, additive drift warnings, provider/season-scoped IDs, no name-only merge, and conflict quarantine.
6. Lifecycle and canonical effects: one envelope per retrieval, ordered append-only events, terminal-state protection, interruption after each contracted stage, idempotent resume/replay, and complete immutable history.
7. Observations and bundles: append-only typed facts linked to derived usable sources, controlled temporal corrections, exactly two deterministic roles, first `usable_at <= cutoff`, stable semantic hash, and honest quality/missingness.
8. Migration and concurrency: PostgreSQL 18.4 base/head and DAT-head matrix, downgrade/re-upgrade, offline SQL without secrets, stable object names/fingerprint, adversarial cross-context constraints, and bounded race tests.
9. Packaging and regression: strict types/lint, marker suites with zero skips, coverage gates, accepted DAT/RUL behavior, and clean installed-wheel replay outside the source tree.
10. Evidence and supply chain: all 25 literal commands (including expected exit 4 and teardown), no live or real provider data, clean final Git provenance, complete baseline patch, detached hashes/CRC, and exact 20-file cap.

P0/P1 findings block Codex completion. Passing tests alone is not acceptance, and human approval remains required before merge/tag.
