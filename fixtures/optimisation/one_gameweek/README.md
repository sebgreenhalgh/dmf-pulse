# OPT-010 reference fixtures

The assurance CLI builds these complete reference/test-only inputs from
`tests.support.optimisation_factories` in a temporary directory. The fixture is deliberately
not a production rules artifact: it is the frozen 2/5/5/3 reference semantics used by the
Stage 10 proof and has no authority to activate 2026/27 manager tactics.
