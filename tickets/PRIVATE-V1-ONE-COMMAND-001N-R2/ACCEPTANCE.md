# PRIVATE-V1-ONE-COMMAND-001N-R2 engineering acceptance

The accelerated path must solve exactly the accepted deterministic three-node private policy:
three consecutive Gameweeks, conditional probability one, no information revelation, unchanged
current-price path, disabled zero terminal value, no chips, and the complete governed retained
incoming shortlist with every currently legal 0/1/2-transfer action at every node.

Profiling must report disclosure-safe structural, transition, memo, Pareto and tactical work by
depth. Any continuation equivalence must include every economically relevant active ownership
cohort, current price, bank, FT and rules identity while retaining complete replayable history on
emitted plans. Future-node batching must evaluate only reachable unique squads with the existing
exact Stage-10 node kernel and reuse the canonical `(node_id, squad_ids)` memo result.

The retained generic `BoundedExactEnumerator` is the oracle. Deterministic and randomized tractable
fixtures must prove exact Decimal and structural equality for recommended policy, root frontier,
baseline, alternatives, FT/hits, bank, squads, tactical plan hashes and tie choices. Hostile tests
must cover history equivalence, purchase-price/bank/FT/current-price/rules distinctions, sell/rebuy,
non-nested roots, future hits, held FT, 0/1/2 frontier, ties, constraints, tactical-cache reuse,
order invariance, fail-closed dispatch and generic branching fallback.

A deterministic live-shaped benchmark must use a 15-player squad, the private STANDARD-scale
incoming universe, three Gameweeks and exact legal 0/1/2 recourse. It must record before/after wall
time and structural/tactical counters, prove exact output equality, complete deterministically and
show a material exact speedup, targeting at least 10x where the measured bottleneck permits.

Acceptance also requires the full Stage-11 unit/golden/property/contract matrix, private rolling,
one-GW, 001M frontier and 001L batching regressions, changed-code branch coverage, Ruff, strict
mypy, frozen sync, build, installed-wheel checks, manifests/repository validation, secret scan and
the repository-required exact-SHA CI. A literal private 3-GW retry is optional only after green CI
and only with credentials and entry ID already present in the operator environment.
