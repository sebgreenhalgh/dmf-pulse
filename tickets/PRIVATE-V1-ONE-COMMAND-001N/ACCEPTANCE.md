# PRIVATE-V1-ONE-COMMAND-001N engineering acceptance

The historical one-Gameweek command remains the default and retains its exact 001M execution,
decision, frontier and hash semantics. Three Gameweeks are selected only through an explicit
horizon option and produce separate rolling contracts bound to the current information cutoff.

The rolling request contains exactly the current, next and following Gameweeks. Every fixture is
officially assigned at that cutoff and passes through the existing Stage-7, score-prior/market,
Stage-8 and Stage-9 pipeline. Missing future markets use the accepted visible `PRIOR_ONLY` Stage-8
mode or block; coverage counts never reinterpret missing evidence as zero. No future actual is an
input. The disclosed topology is a deterministic three-node chain with no fabricated revelation.

Stage 11 supplies exact compiled FT arcs, hit costs, integer bank/selling-price transitions,
ownership-spell continuity, legal future transfers and exact tactics. Only the root decision is
`DO NOW`; both future decisions are `PROVISIONAL - REOPTIMISE AT THAT DEADLINE`. Terminal value is
the accepted disabled `ZERO_FLEXIBILITY_BASELINE_V1` after GW+2. Prices remain current-price
constant. There is no FT bonus, chip action, rank utility, or global optimality claim.

The root frontier selects the best complete three-Gameweek policy at each available root transfer
count and reports immediate and horizon value against the same no-root-transfer comparator. The
one-GW versus three-GW explanation is a deterministic utility decomposition; it does not invent
causality. Paired horizon uncertainty uses common within-Gameweek Stage-9 scenarios and an
explicitly disclosed independent cross-Gameweek product because no accepted cross-Gameweek
transition model exists.

Acceptance requires focused RED-to-green tests for the basic policy, saved-FT root reversal, the
opposite decision, rational hits, every compiled FT transition, price/bank/spell/squad continuity,
root-only actionability, leakage rejection, nonanticipativity, terminal and price modes, future
market missingness, order/tie invariance, bounded-search wording, 001M compatibility, and exact
agreement with the independent tiny Stage-11 oracle. Run affected suites, branch coverage, Ruff,
strict mypy, frozen sync, build, installed-wheel/CLI, repository/manifest and secret gates, then
require the pushed exact SHA to pass repository CI.
