# R9C-A2 acceptance

R9C-A2 may prepare one private transient three-Gameweek context from the existing one-command
acquisition and assembly path, compile the frozen R9B posterior shadow from the same in-memory
official-FPL snapshot, and run four fresh canonical rolling services. STALE must remain literal
default `PrivateV1RollingRecommendationService()` construction. The three shadow worlds use the
sealed A1 resolver and remain `SHADOW_NOT_MODEL_INPUT`.

The preparation context and live observation are private internal contracts. They have no CLI,
environment, configuration, persistence, cache, backup, training, calibration or active-service
selection route. Provider access ends before the four solves begin. A live result is terminal
operator output only and is never repository evidence.

The one-shot ticket attestation `PRIVATE-V1-ONE-COMMAND-001N-R9C-A2#ONE-SHOT-2026-09-17`
authorizes only an attempt after independent provider-purpose and implementation review,
publication and fully green exact-SHA CI. It does not amend provider rights. A blocked attempt is
terminal and must not retry automatically.
