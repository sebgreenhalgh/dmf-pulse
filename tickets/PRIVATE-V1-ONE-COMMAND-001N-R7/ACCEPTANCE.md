# R7 exact acceleration contract

Immutable baseline: `5878a39448456df0d07d58823e6dfa7c8e574715`; exact-SHA CI
`34503653544` verified complete with all 12 jobs successful.

Profile before optimising. Reproduce 12 candidates/node, 30+ root actions, 30+
second-node predecessor states, approximately 1,000 terminal economic states,
tens of thousands of actions and thousands of overlapping fixed squads. Separate
action enumeration, transition, memo, Pareto, tactical kernel, batch overhead and
canonical final evaluation. Measure perf_counter wall and process_time CPU.

Preserve R6 V3 screen module bytes, sets, hash, root/future scope, FT recourse,
all tactical configurations/distributions/ties/hashes, complete frontiers,
comparator, alternatives and attribution on frozen inputs. No heuristic pruning.
Canonical winning tactical verification remains mandatory. Generic reduced and
larger feasible exhaustive oracles must equal the accelerated candidates exactly.
Production-shaped frozen R6-fast and R7-fast results must be semantically identical.

Investigate terminal equivalence, layered exact DP, node-wide batches, overlapping
squad primitive reuse, exact incremental evaluation and admissible bounds. Implement
only proven exact improvements. Terminal coalescing, if used, requires hostile tests
for closed history, active purchase cohorts with same/different sale proceeds,
bank, FT, squad, node scope, nonzero terminal value and paid-transfer fallback.
Replay chosen suffixes against each caller's full original history.

Track cumulative state expansions, legal actions, action combinations, unique
node/squads, tactical evaluations and logical/factored tactical work. Derive STANDARD
guards from measurements, never reject the observed R6 shape merely to hide slowness.
Fail before tactical work where guaranteed cap violations are known. Diagnostics
contain safe structural counts and wall/CPU timings, excluded from semantic hashes.

Require materially greater than 5x measured exact acceleration of the reproduced
bottleneck (target 10x); if 5x cannot be achieved, report the measured residual
bottleneck and do not claim completion. Direct overlapping-squad benchmark must
verify every result against R6 and report squads/sec and kernel counters.

Run new performance/equivalence/replay/kernel/batching/oracle tests, R2/R4/R5/R6,
one-GW/private rolling regressions, changed-code branch coverage, Ruff, strict mypy,
frozen sync, build, external installed wheel, manifests/repository validation,
secret scan and git diff check. Regenerate active manifests before publication.
Require all exact-SHA CI jobs. Keep resumable Git/evidence checkpoints.

Optional live retry only after green CI and demonstrated practical runtime with
already-present runtime inputs. Never request or persist credentials. Changed live
cutoffs are not semantic equality evidence or causal evidence for R4/R6 differences.
No PR, merge, tag, activation or future-market/model-quality work.
