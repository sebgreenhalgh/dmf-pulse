# PRIVATE-V1-E2E-001A engineering acceptance

This ticket is implemented only when one public application service and CLI surface execute a
strict, deterministic, current-input pipeline through the existing Stage 8, Stage 9, Stage 11,
Stage 10 and captain/vice boundaries. The offline synthetic proof is mandatory but cannot be
presented as a genuine current recommendation.

The decision must retain the canonical Gameweek player-by-scenario matrix, use the same scenarios
for the recommendation and no-transfer baseline, apply transfer hits once, apply captain scoring
once in the accepted tactical evaluator, and verify the selected captain/vice pair through the
accepted captaincy service. The current action may be `NO TRANSFER`; chip action is always the
explicit `NO CHIP` in this ticket.

Every source, policy, result and report is content-bound. Replay is network-free, validates every
declared digest, does not depend on an absolute developer path, and reproduces the exact semantic
decision hash. Real FPL, Odds or manager payloads may be frozen only where their governing rights
profiles explicitly permit retention. Otherwise the real-run/replay acceptance gate remains
blocked and the implementation must report the exact rights/input blocker.

The engineering handoff status is
`PRIVATE_V1_E2E_001A_IMPLEMENTED_PENDING_INDEPENDENT_REVIEW`; activation remains
`NOT_PRODUCTION_ACTIVE`. Neither string records independent review, human acceptance, merge or
production activation.
