# R9B acceptance contract

Parent `b4d4c774ac4150a6fbdeb579c143a0458fa9156b`. The resumed user instruction
extends the original ticket with aggregate diagnostics and prioritized offline
experiments, not live activation or a rights amendment.

Authority: `specs/manifests/authority_manifest.json` scopes A4-FPL-ingestion and
A9-points; DMFP-04 section 3 atomic rights/no implied permission; accepted
ADR-ASSUR-006 provider-use enforcement and ADR-ASSUR-004 named random streams;
DMFP-09 sections 6.5 and 7 distinguish goals, penalties and FPL assist classification.
The assist bridge is a candidate propensity experiment, not a replacement assist
classifier or a calibrated conversion. Existing classification remains unchanged.

Historical numerical authority: Git object
`b4353dbdcebd31f2a807bee90ec04b3ee8b07389`, GW1-PLY-002 calibration method/result
and GW1-PLY-003 three posterior artifacts. Fixed temporary kappa sensitivity only;
no current-data fitting, no winning-world selection, no epsilon or double shrinkage.

Required engineering gates:

- Recompute three historical source digests; pin 599 x 3 ordered donor/world pairs.
- Revalidate current R9A evidence, complete current binding, cutoff and finalized
  acquired window. Fail reconciliation, identity, malformed or integrity conflicts.
- Test observed zero, missing field, missing player row, zero-minute discipline,
  structural outfield saves, degenerate priors, overflow and fallback isolation.
- Only shadow assist propensity, yellow/red rates and GK saves can change. Total
  goals are a separate diagnostic. Do not split CBI or invert BPS.
- Deterministic sealed all-world output and aggregate diagnostics; no live rows,
  identities, bodies, credentials or manager facts in safe summaries/evidence.
- Tests, changed-code branch coverage, active frozen parent differential, unchanged
  endpoint surface, strict mypy/Ruff, frozen sync, build, external installed wheel,
  canonical repository manifests, validator, secret scan and diff check.
- Hostile self-review, fresh independent read-only review with
  CLEAR_FOR_PUSH_AND_EXACT_SHA_CI, exact local/remote SHA and mandatory CI green.

R9C activation readiness is a draft only. No live probe is executed in R9B.
