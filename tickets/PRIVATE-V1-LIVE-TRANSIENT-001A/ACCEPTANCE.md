# PRIVATE-V1-LIVE-TRANSIENT-001A engineering acceptance

The ordinary current manager and unified source APIs continue to require an `ACTIVE` target
ruleset. A `VERIFIED` ruleset is admitted only when the caller supplies a self-hashed authority
bound to the exact ruleset and complete `FULL_SEASON` capability, an approval time no later than
the decision cutoff, private transient purpose, no persistence, and `NOT_PRODUCTION_ACTIVE`.
The authority cannot publish or activate rules.

The packaged player-prior and acceptance resources remain byte-identical and GW1-only. For a
target Gameweek after GW1, execution requires a separately sealed
`PRIVATE_CURRENT_GW_STALE_PRIOR_CARRY_FORWARD_V1` policy. A same-player profile is retained only
when its official-FPL identity and team relation still match. Every invalid or missing relation
requires an explicit same-position donor whose governed goal, assist and auxiliary lineages are
all `FPL_POSITION`; otherwise that player fails closed. The original 2026-08-21T17:30:00Z cutoff,
grade E, `CANDIDATE_NOT_ACCEPTED`, GW1-only historical acceptance and absence of new current
history remain visible.

`dmf private-v1 live-transient` reads operator-owned bootstrap, fixtures and manager declaration
files through the accepted bounded manual compilers, composes accepted current Odds and identity
inputs, rebuilds current market constraints, accepts only authenticated current score-prior
bundles, applies manual transient Stage 7, and invokes the existing private-v1 Stage 8-11 and
captaincy path. It has no freeze, output-file, database-write, cache or backup option. It displays
the transient report, creates zero persistent artifacts, and explicitly reports
`FORBIDDEN_BY_CURRENT_RIGHTS_PROFILE` replay retention.

A synthetic replay remains retention-authorised and unchanged. A real recommendation is complete
only if genuine operator inputs are present and the command actually produces the full legal
decision. Engineering readiness alone does not imply real-run completion, independent review,
human acceptance, merge, or production activation.
