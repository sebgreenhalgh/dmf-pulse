# CURRENT-PLAYER-ALLOCATION-PORT-001A acceptance contract

This file defines engineering gates. It does not record human acceptance.

## Lineage and safety

- The branch descends from `ddd74fe38ddfae3733ad5189b417123477e9f23e`; parent exact-SHA CI
  run `33413309522` is successful.
- Donor inspection and preserved resources are pinned to
  `f4d75dc5b107901a3619f136c3d3d7d1d7632a3c` without merge, rebase, or wholesale cherry-pick.
- Donor private GW1 acceptance remains historical evidence only. This port remains pending
  independent review and not production-active.

## Prior and identities

- The exact donor central allocation artifact and its exact acceptance record load from the wheel
  through strict, extra-forbid, hash-validating contracts.
- The old transient donor UUIDs are integrity evidence only. Runtime profiles bind by exact
  official-FPL source identities to explicit current canonical Stage-7 UUIDs; ambiguity, stale
  team membership, missing coverage, duplicate IDs, malformed data, and tampering block.
- Donor cutoff, scope, weak/role fallback lineage, limitations, and confidence are preserved.

## Allocation and scoring

- Every sampled Stage-8 scoreline is conserved exactly by credited player goals plus benefiting
  opponent own goals, with no double-counted penalty goal.
- Scorers, assisters, own-goal players, penalty takers, and goalkeepers are correct-team and on
  pitch at event time; scorer and assister differ; saves are goalkeeper-only and linked to
  compatible on-target attempts where represented.
- Fixed semantic inputs and seed produce identical allocation and Stage-9 result identities.
- The current rules engine produces integer scenario scores, joint BPS/bonus, a retained
  player-by-scenario matrix, and marginals derived from that matrix.

## Vertical slice and quality

- Offline integration proves private-manual Stage 7 -> `Stage7MinutesContext` -> current
  `ScoreDistributionService.project()` -> current allocation -> canonical Stage-9 points.
- Direct coverage also proves the empirical-Bayes Stage-7 family remains accepted.
- Focused, property, integration, inherited, format, lint, strict typing, diff, repository,
  secret, build, installed-wheel/resource, branch-coverage, and exact-final-SHA CI gates pass.
- No unresolved P0, P1, or material P2 remains after adversarial self-review.
