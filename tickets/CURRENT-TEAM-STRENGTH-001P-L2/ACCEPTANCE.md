# L2 Phase A acceptance

Parent `4d712ecf84e9c1f01a0f29354bd93c5296befb35`; direct isolated branch.
Latest human instruction is controlling: prepare and publish only; the agent
must not invoke the live observation. FPL/Odds requests and credential inspections
remain zero throughout Phase A.

Preserve D1 comparison, diagnostics, input controls, candidate/CRN semantics,
Stage 8–11 mathematics, transport gate, ordinary command and model selection.
Only active authority, necessary Odds purpose metadata and L2 run/status/operator
identity change. Historical L1 approval stays in the explicit consumed set.

Require exact pair/mismatch/consumed tests; exact FPL and Odds semantic hashes;
purpose-only Odds diff; unchanged FPL/OpenFootball bytes; credential/public-preflight
blocks before consumption; inherited first-send/no-retry tests and generated-data
comparison; D1 and public 001A/readiness/Stage 8–11 regressions. Unit tests use no network.

Quality: Ruff format/check, strict mypy, frozen offline sync, build wheel/sdist,
outside-source installed-wheel smoke, repository validator, canonical manifests,
first-party secret scan, capped safe review pack and git diff whitespace checks.
Inherited mandatory exact-SHA CI must pass, with local/remote equality and clean tree.

Fresh independent verdict:
`CLEAR_FOR_CURRENT_TEAM_STRENGTH_001P_L2_ONE_SHOT_EXECUTION`.
This is readiness for the human's Phase B, not an instruction for the agent to run it.

## Human operator handoff

The existing `scripts/run_team_strength_l1.py` filename and internal class names
are retained to minimize code movement; its current exact authority and safe
status identity are L2. No generic approval selector or reset is introduced.
Use the final green L2 SHA, not the parent, in `--code-sha`.

Authenticate/reassess public readiness at Phase A handoff and again inside the
existing operator service at the actual Phase B cutoff. Require LIVE_OBSERVED,
non-STALE_BLOCKED state, zero missing due, full 380 current fixtures and retained
Hessian/covariance. A public-only block leaves L2 unconsumed.

Run `observe` once in a terminal, without redirection, tee, scheduler or wrapper
retry. The inherited process-local guard is not a durable cross-process ledger.
After the first attempted private request, never rerun under this approval,
regardless of outcome. No private input/response/comparison persistence is permitted.
