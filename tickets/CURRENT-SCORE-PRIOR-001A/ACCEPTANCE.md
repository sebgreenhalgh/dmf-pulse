# CURRENT-SCORE-PRIOR-001A acceptance

Engineering acceptance requires all of the following:

1. The branch descends exactly from merged main
   `7609f041f4f7a415ef58f0f5c682a9b1d5b16d49` and does not touch the unrelated dirty
   CURRENT-AVAILABILITY worktree.
2. Rights profile `openfootball_football_json_score_prior_v1` version `1.0.0` records the exact
   2026-08-30 human approval, every repository capability, conservative private-output policy,
   CC0 licence blob, retention position and truthful approver.
3. Missing, non-approved, wrong-purpose or insufficient rights fail as `RIGHTS_BLOCKED` with
   `transport_call_count = 0`.
4. Transport can request only the approved HTTPS host, immutable commit and four allowlisted
   resources; redirects, oversized/truncated bodies, unexpected content and TLS/HTTP failures are
   typed and disclosure-safe.
5. Every resource matches its configured raw byte count, SHA-256 and Git blob SHA-1 before use.
6. The parser rejects duplicate keys, excessive depth, unknown structures, non-integer score
   values, invalid periods and malformed identities without coercion or silent zero filling.
7. Direct arrays are accepted only for this Premier League regular-season contract; each season
   has 380 valid fixtures, 20 teams, 38 appearances per team and 380 unique date/home/away keys.
8. The selected seasons total `N=1140`, home goals `1839`, away goals `1567`; exact Decimal
   `ROUND_HALF_EVEN` quantization emits `1.613158` and `1.374561`.
9. The result is `RECONSTRUCTED`, cannot backdate the source commit as `usable_at`, and requires
   approval/receipt/validation/usable timestamps no later than the information cutoff.
10. The emitted `ScorePriorRequest` is labelled weak league-level support, remains structurally
    independent of markets, and makes no current team-strength or production-calibration claim.
11. No database read/write, source persistence, Stage-7 substitution, GCS-008 projection, player
    allocation, Stage 9, optimisation, public display, redistribution or production activation is
    performed.
12. Focused branch coverage is at least 90%; inherited GCS-008, static, build, installed-wheel,
    repository, manifest and secret gates pass.

Engineering status after these gates:

`CURRENT_SCORE_PRIOR_001A_IMPLEMENTED_PENDING_INDEPENDENT_REVIEW`

Next action: `INDEPENDENT_REVIEW_CURRENT_SCORE_PRIOR_001A`.
