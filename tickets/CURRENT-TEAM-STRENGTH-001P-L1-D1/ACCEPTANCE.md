# D1 execution boundary

Parent `06cbd10719fee3cde87dbb2f9698d81567672618`, direct isolated branch.
The user-supplied D1 contract controls; approved DMFP authority is unchanged.

The human explicitly resolved the discovered control-identity conflict:
"Correct the control identity now; preserve decisions, projections and
classifications, but allow comparison hashes to change."
This authorizes only input/derived identity separation, not any changed solve.

Required evidence: every major diagnostic boundary injected offline; closed safe
terminal fields; per-world started/completed proof; actual Stage-8 full/H2H/prior
range tests and unchanged totals-only semantics; all 24 control audit; random
input alignment and genuine screen-derived limit regression; successful five-case
001P differential against the exact parent (only authorized identity change).

Required quality: new-diagnostics branch coverage >=90%; inherited 001P, L1
generated-data vertical slice, A2, ordinary one-command, public 001A/readiness,
Stage 8/9/10/11/property regressions; Ruff, strict mypy, frozen offline sync,
wheel/sdist build, outside-source installed-wheel smoke, repository validator,
canonical manifests, first-party secret scan and diff whitespace check.

No PostgreSQL requirement is introduced. Existing CI database gates remain
separate inherited acceptance. No test may use a live provider or credentials.

Fresh independent review must resolve all P0/P1/material P2 findings. Its verdict
is readiness for a separate fresh-authorization decision, never authorization to
execute, retry or activate a model.
