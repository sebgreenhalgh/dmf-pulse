# PRIVATE-V1-ONE-COMMAND-001N-R1 engineering acceptance

The five-minute acquisition cutoff remains unchanged. Current FPL, current odds and the exact
commit-pinned OpenFootball source prior must be acquired and made usable no later than that cutoff.
The authenticated `CurrentScorePriorResult` is then retained only in memory while identity,
Stage-7 and later deterministic work proceeds, and the same result is bound to every exact current
and future fixture context without another source call.

Acceptance requires a live-shaped injected-clock regression in which all external inputs arrive
within the five-minute window, Stage-7 advances time to more than five minutes after the cutoff,
and the complete rolling input reaches recommendation execution. The regression authenticates the
original request/receipt/validation/usable timestamps, four-call transport count, source hash,
fixture-specific bundles, no post-Stage-7 transport and timing-invariant sealed semantics.

Inherited source tests must continue to reject acquisition or receipt after cutoff, unavailable
rights, source usability after bundle `as_of`, malformed provenance and unexpected transport
failure. Existing one-GW and three-GW tests must retain decision, FT, bank, squad, horizon,
terminal, price, no-information-revelation, candidate-pruning, no-chip and rank-off semantics.

Run focused orchestration and score-prior tests, one-command and rolling compatibility suites,
branch coverage for changed production code, Ruff, strict mypy, frozen sync, build, installed-wheel
checks, both active/ticket manifest validations, repository validation, secret scan and the exact
pushed repository CI. No PR, merge, tag, activation or private provider material is permitted.
